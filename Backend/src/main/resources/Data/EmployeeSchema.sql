CREATE TABLE user(
    id INTEGER AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(20),
    email VARCHAR(30),
    branch VARCHAR(20),
    role VARCHAR(20),
    type VARCHAR(20),
    dept VARCHAR(20),
    password VARCHAR(20)
);